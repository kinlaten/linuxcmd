Memory note: when adding a `#` comment to a command block, leave a blank line after that command so the snippet stays easy to scan.

When write note for adb command, give some example output and note about it
